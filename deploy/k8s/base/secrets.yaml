# WARNING: This file contains placeholder values only.
# Replace "CHANGE-ME" with real secrets BEFORE deploying.
# For production, consider using SealedSecrets, ExternalSecrets,
# or creating secrets imperatively:
#   kubectl create secret generic artifact-keeper-secrets \
#     --from-literal=db-password=$(openssl rand -base64 32) \
#     --from-literal=jwt-secret=$(openssl rand -base64 64) \
#     --namespace artifact-keeper
apiVersion: v1
kind: Secret
metadata:
  name: artifact-keeper-secrets
  namespace: artifact-keeper
type: Opaque
stringData:
  db-password: "CHANGE-ME"
  jwt-secret: "CHANGE-ME"
  # For S3 storage (optional)
  s3-access-key: ""
  s3-secret-key: ""
