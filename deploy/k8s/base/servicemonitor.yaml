apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: artifact-keeper
  namespace: artifact-keeper
spec:
  selector:
    matchLabels:
      app: backend
  endpoints:
    - port: http
      interval: 30s
      path: /metrics
